# Legacy TensorFlow archive

These files are retained from the original `mpox` repository (February 2025):

- `152.py` — ResNet152 training and evaluation script
- `DENSENET169.py` — DenseNet169 training and evaluation script
- `ACCURACY.tif` — archived evaluation figure
- `paperdraft.pdf` — archived paper draft

They use local Windows dataset paths and TensorFlow/Keras. The current application is the PyTorch-based full-stack implementation at the repository root.
